# list -> mutable
x = [ 1, 2, 3, 4, 5]

# tuple -> immutable
x = ( 1, 2, 3, 4, 5 )

# dictionary (key-value pairs, like associative/hash array)
x = { "a": 1, "b: 2, "c": 3 }

# set -> unordered list of unique values
x = { 1, 2, 3, 4, 5}

# these structures can contain any object, 
# and can be used to represent simple or complex structured data
